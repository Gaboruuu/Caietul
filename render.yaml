services:
  - type: web
    name: caietul-backend
    runtime: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false
